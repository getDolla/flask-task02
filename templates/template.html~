<!DOCTYPE html>
<html>

  <head>
    <title> {{ title  }} </title>
    <style>
      body { text-align: center; }
    </style>
  </head>
  <body>
    {% for item in collection %}
     {{ item }}
     <br>
    {% endfor %}
  </body>

</html>
